tpu-info==0.7.1
yapf==0.43.0
pytest
pytest-mock
absl-py
numpy
google-cloud-storage
jax==0.10.1
jaxlib==0.10.1
libtpu==0.0.41
jaxtyping
# Temporarily restrict fastapi version due to https://github.com/vllm-project/vllm/pull/45629
# TODO(lk-chen): remove once LKG moves past above commit
fastapi[standard]<0.137.0
flax==0.12.4
torchax==0.0.11
qwix==0.1.2
# Marin's TPU-vLLM stack resolves torch==2.11.0 with torchvision==0.26.x.
# Keep this line broad enough for that CPU wheel pair while staying on the
# matching TorchVision minor.
torchvision>=0.26.0,<0.27
pathwaysutils
parameterized
numba==0.62.1
runai-model-streamer[s3,gcs]==0.15.9
gcsfs==2026.1.0
hypothesis==6.151.9
sortedcontainers==2.4.0
# Marin/Levanter currently stay on Transformers 4.x, while the selected vLLM
# LKG only requires >=4.56. Keep source installs lockable with Marin without
# forcing all training paths onto Transformers 5.x.
transformers>=4.57.1,<5.0
