# CPU-only subset of the TPU serving stack: enough to import `tpu_inference` and run
# the Marin delta's CPU tests on a GitHub-hosted runner.
#
# requirements.txt cannot be used directly -- it pins libtpu, which needs a TPU.
#
# vLLM is not optional here even for an import: tpu_inference/__init__.py pulls it in,
# so every `tpu_inference.` import needs it. This is stock PyPI vLLM, not Marin's vLLM
# fork, which is a source build far too slow for PR CI. Marin's fork is what production
# and the nightly use; the gap is why tests/models/common/test_model_loader.py runs in
# the nightly and not here (it imports a quantization module only the fork ships).
#
# Install with `uv pip install --torch-backend cpu -r` so torch resolves to the CPU
# wheel instead of the CUDA one.
jax[cpu]==0.10.1
flax==0.12.4
torchax==0.0.11
qwix==0.1.2
transformers>=5.8.0
vllm==0.25.1
jaxtyping
numpy
requests
safetensors
pytest
pytest-mock
